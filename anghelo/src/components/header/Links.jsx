export const Links = ({ text }) => {
  return (
    <a className="px-4 py-2 mt-2 text-base text-textlight font-semibold  md:mt-0 md:ml-4 transition ease-in-out duration-500 hover:text-primary focus:text-primary" href="#">{text}</a>
  )
}
